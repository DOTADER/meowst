package com.meowstgdx.game.entities.animals.passives.Water;

import com.meowstgdx.game.entities.animals.Animal;

public class Clomp extends Animal { // Crab-claw type creature, clamps mouth shut every few seconds and can only be attacked from the inside
    protected Clomp(float moveSpeed, float attackSpeed, int health, int maxHealth, int stamina, int maxStamina, int damage, int protection) {
        super(moveSpeed, attackSpeed, health, maxHealth, stamina, maxStamina, damage, protection);
    }
}
